name: Release

on:
  release:
    types: [published]

permissions:
  contents: write
  actions: write

env:
  QT_VERSION_WASM: '6.10.1'
  QT_VERSION_HOST: '6.10.1'
  QT_MODULES: 'qtmultimedia qtquick3d qtshadertools qt5compat qtimageformats'
  EMSDK_VERSION: '4.0.7'

jobs:
  # Release quality gate (SECURITY.md): a high/critical CVE against the
  # pinned Qt blocks the runtime upload until Qt is bumped or the CVE id is
  # explicitly waived via the QT_CVE_WAIVE repository variable.
  cve-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Qt CVE gate
        env:
          QT_CVE_WAIVE: ${{ vars.QT_CVE_WAIVE }}
        run: |
          set -o pipefail
          python3 .github/scripts/qt_cve_check.py \
            --qt "${{ env.QT_VERSION_WASM }}" --fail-on high | tee -a "$GITHUB_STEP_SUMMARY"

  build-wheel:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.x'

      - name: Inject version and build wheel
        working-directory: tools/clay-dev-server
        run: |
          VERSION=$(cat ../../VERSION)
          sed -i "s/^version = \"0.0.0\"/version = \"${VERSION}\"/" pyproject.toml
          pip install build
          python -m build --wheel

      - name: Upload wheel to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" \
            tools/clay-dev-server/dist/*.whl

  build-webdojo:
    needs: cve-gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: 'recursive'

      - name: Setup Emscripten
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: ${{ env.EMSDK_VERSION }}

      - name: Install Qt (Desktop - for host tools)
        id: qt-desktop
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION_HOST }}
          host: linux
          target: desktop
          arch: linux_gcc_64
          modules: ${{ env.QT_MODULES }}
          set-env: false
          cache: true
          dir: ${{ runner.temp }}/qt-desktop

      - name: Install Qt (WASM)
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION_WASM }}
          host: all_os
          target: wasm
          arch: wasm_multithread
          modules: ${{ env.QT_MODULES }}
          aqtversion: '>=3.2.0'

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build
          pip install build

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
          working-directory: ./docs

      - name: Configure CMake
        run: |
          "$QT_ROOT_DIR/bin/qt-cmake" -B build -GNinja -DCMAKE_BUILD_TYPE=Release \
            -DCLAY_BUILD_WEBSITE=ON \
            -DQT_HOST_PATH="${{ runner.temp }}/qt-desktop/Qt/${{ env.QT_VERSION_HOST }}/gcc_64" .

      - name: Build Web Runtime + starter bundle
        run: cmake --build build --target clayground_starter

      - name: Upload Web Runtime assets to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" \
            build/bin/clayground.js \
            build/bin/clayground.wasm \
            build/bin/qtloader.js \
            build/clayground-starter.zip

  trigger-docs-rebuild:
    needs: build-webdojo
    runs-on: ubuntu-latest
    steps:
      - name: Trigger docs rebuild to include release WASM on Pages
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run docs-check.yml --repo "$GITHUB_REPOSITORY" --ref main
